# Dotfiles

## Run

```bash
curl -fsSL https://raw.githubusercontent.com/kalescky/dotfiles/main/bootstrap.sh | bash
```

This installs/updates Pixi, syncs this repo to `~/.dotfiles`, and runs `bootstrap.yml`.

